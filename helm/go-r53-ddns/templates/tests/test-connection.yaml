apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-r53-ddns.fullname" . }}-test-connection"
  labels:
    {{- include "go-r53-ddns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-r53-ddns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
